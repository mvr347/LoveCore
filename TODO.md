# Что осталось доделать

Список после аудита незаконченных механик экосистемы Love*. Всё, что было в аудите сделано и
слито, здесь не повторяется — остались только те работы, которые сознательно отложены.

Разделено по тому, чего работа требует: первый раздел — механическая, её можно брать в любом
порядке; второй — недоделки внутри плагинов; третий — вопросы, на которые сначала нужен ответ
владельца, а не код.

---

## 1. Этап 2 ядра: перевести соседей на `lovecore-api`

Ядро заведено (`fe15e01`), но ни один сосед на него ещё не переведён. Пока этого нет, ядро —
шесть контрактов, которыми никто не пользуется, а три оракула внутри ядра держатся на
рефлексии по соседям.

Порядок важен: сначала соседи начинают **сообщать** о себе (`StatBus`), потом — **отвечать** за
свои данные (оракулы), и только после этого рефлексия из `lovecore-plugin/integration/Neighbour`
удаляется.

### 1.1. LoveLeaderboards → `StatBus` вместо трёх рефлексий

Самое ценное из всего списка: именно здесь аудит нашёл три интеграции, которые **никогда не
работали** (см. таблицу в `README.md`).

| Файл | Что делает сейчас |
|---|---|
| `integrations/LoveHuntIntegration.java:23` | `Class.forName` по имени класса события, `getMethod("getKiller")` |
| `integrations/LoveClansIntegration.java:49,92,118` | `getAllClans`, `LoveClansAPI.getInstance`, `getPlayerClan` рефлексией |
| `integrations/RatingSyncIntegration.java:57,77,114` | `getHunterRatings`, `getBrewerSkillsAsync` рефлексией |

Что сделать: подписаться на `StatChangedEvent` и складывать значения по именам из `Metrics`.
Все три класса интеграций после этого удаляются целиком — вместе со своей рефлексией и пустыми
`catch`. Категория топа становится строкой конфига плюс константой в `Metrics`, а не новым
классом.

Обратная сторона: топ теперь наполняется только тем, о чём сосед сообщил сам, поэтому
переводить надо парой — сразу с 1.2.

### 1.2. Источники метрик: сообщать о событиях самим

Каждому — один вызов `LoveCore.service(StatBus.class).ifPresent(...)` в точке, где событие уже
обработано:

- **LoveHunt** — `BOUNTIES_COMPLETED`, `HUNTER_RATING` (рейтинг уже считается, витрина есть).
- **LoveBrew** — `BEVERAGES_BREWED`, `BREWER_SKILL`.
- **LoveTrades** — `TRADES_COMPLETED`.
- **LoveClans** — `CLAN_INFLUENCE`, `CLAN_WARS_WON`, `CLAN_TERRITORIES`, `CLAN_WEALTH`.

### 1.3. Оракулы: сосед реализует контракт сам

Сейчас `ClansProfileOracle`, `ClaimsTerritoryOracle` и `BehaviorReputationOracle` собраны в ядре
рефлексией. Когда сосед реализует интерфейс сам и зарегистрируется в `ServicesManager` с
приоритетом выше `Normal`, он вытеснит реализацию ядра **без правок в ядре** — так это и
задумано.

- **LoveClans** → `ProfileOracle`
- **LoveClaims** → `TerritoryOracle`
- **LoveBehaivor** → `ReputationOracle`

После всех трёх: удалить `integration/Neighbour` и три реализации оракулов из
`lovecore-plugin`.

### 1.4. Потребители: `CombatState` вместо метки `in_combat`

- **LoveTweaks** — свитки телепортации читают метку напрямую.
- **LoveTrades** — запрет торговли в бою (добавлен в аудите) читает метку напрямую.

Выгода не в красоте, а в том, что ядро знает про осаду и войну, чего метка не знает:
`combatSource()` даёт причину отказа «нельзя торговать во время осады» вместо общего «в бою».

### 1.5. LoveShop: `ReputationOracle` вместо строки `"good"`

LoveShop ищет рефлексией `LoveBehaviorAPI` и сравнивает результат со строкой `"good"`, отчего
репутация фактически бинарна: всё, кроме `"good"`, даёт ровно ноль. `Tier` возвращает шкале
пять ступеней.

### 1.6. Единый кошелёк — последним

`LoveEconomy` в ядре есть и с журналом операций, но у скупщика, клановой казны и пивовара
сегодня деньги свои. Перевод затрагивает балансы живого сервера, поэтому идёт последним и с
миграцией существующих балансов, а не «заодно».

---

## 2. Недоделки внутри плагинов

### 2.1. LoveBrew: выдержка не влияет ни на что

Самая крупная недоделка из оставшихся: бочки есть, процесс выдержки есть, а на напиток он не
попадает.

- `models/AgingProcess.java:75` — `getQualityMultiplier()` честно считает `0.8…1.2` по попаданию
  в окно выдержки. **Никто не переносит результат** в `Beverage`: единственные вызовы
  `setAgingQualityMultiplier` — чтение из базы (`BeverageRepository.java:104`), то есть в поле
  всегда остаётся 1.0.
- Из-за этого слагаемое выдержки в `QualityCalculator.calculatePurity` (строка 48) всегда даёт
  ровно ноль — чистота напитка, досчитанная в аудите, работает на двух слагаемых из трёх.
- `QualityCalculator.calculateAgingQuality` (строка 60) не вызывается ниоткуда — мёртвый код,
  написанный ровно под эту задачу.
- `AgingProcessManager` (53 строки) держит процессы **только в памяти**: бочки в базе есть
  (`barrels`), процессы — нет. Рестарт сервера теряет всю выдержку. `tick()` при выгрузке чанка
  молча выбрасывает процесс, `shutdown()` очищает карту.
- `BarrelListener.java:115` жёстко зашивает срок выдержки в 7 суток и размер `MEDIUM`, игнорируя
  фактический размер бочки и тип напитка.
- `PlayerStats.maxAgingDays` не пишется никогда.

Что сделать: сохранить процессы в базу рядом с бочками; на завершении переносить
`getQualityMultiplier()` в напиток и ставить `agedAt`; брать срок и размер из бочки и рецепта, а
не из константы; удалить `calculateAgingQuality` либо вызвать его там, где он задумывался.

### 2.2. LoveHunt: удержанный процент уничтожается

`service/BountyService.java:356` — при отмене и истечении контракта удержанная часть награды не
уходит никуда: её просто не выплачивают. В коде это отмечено `TODO` с оговоркой «пока нет
внутренней валюты».

Внутренняя валюта теперь есть — `LoveEconomy`. Естественное решение: перевести удержанное на
счёт сервера (или клана-владельца территории) с причиной в журнале, а не терять. Зависит от 1.6.

### 2.3. LoveClaims: устаревший `TeleportCause.CHORUS_FRUIT`

`listener/ProtectionListener.java:193` — помечен `deprecated for removal` в Paper. Мелочь на
пять минут, но при обновлении API сломается компиляция.

---

## 3. Решения, которых ждёт код

Здесь нужен ответ владельца: любой из вариантов реализуем, но выбор меняет игру, а не только
код.

### 3.1. LoveClans: геометрия территории лежит в двух местах

`storage/DatabaseManager.java:180` — таблица `clan_territories` хранит **и** свою рамку
(`min_x…max_z`), **и** ссылку `advanced_claim_id` на приват LoveClaims. Два источника правды об
одних и тех же границах: сдвинули приват в LoveClaims — рамка клана осталась прежней, и дальше
два плагина по-разному отвечают, чья это земля.

`TerritoryOracle` в ядре уже написан из предположения, что ответ **один**: «приват клана и
приват игрока не пересекаются и не вкладываются друг в друга — это одна сущность в одном
реестре, различаемая типом».

Варианты:

1. **Свести к ссылке.** `clan_territories` оставляет `claim_id`, `clan_id` и своё (баннер,
   имя, `pvp`, `is_capital`), а геометрию всегда спрашивает у LoveClaims. Требует миграции и
   делает LoveClaims обязательным для клановых территорий.
2. **Оставить как есть**, но добавить сверку при старте и явную починку рассинхрона.

Первый вариант честнее, второй дешевле. Пока решения нет, `TerritoryOracle` отвечает по тому
источнику, который нашёлся первым.

### 3.2. LoveTweaks: голубь не знает про территории

`post/PostManager.java:34` — прямо в документации класса: «интеграции с LoveClaims здесь нет,
голубь летит к последней известной точке получателя на этом сервере». То есть посылка проходит
сквозь вражеский приват и осаждённую территорию так же спокойно, как над ничьей землёй, и
доставляется в точку выхода получателя, а не в его дом.

`TerritoryOracle.hostileFor(playerId, location)` написан в том числе под этот случай.

Варианты:

1. **Голубь уязвим над вражеской землёй** — над враждебной территорией его можно сбить, груз
   выпадает (механика сбивания уже есть: `PostFlight`, «looting, if shot down»).
2. **Голубь не летит во враждебное** — отказ на этапе отправки с внятной причиной.
3. **Оставить нейтральным** — почта вне политики, сознательно.

Третий вариант тоже решение, и его достаточно просто зафиксировать — тогда комментарий в
`PostManager` перестаёт читаться как недоделка.

### 3.3. Голубь и последняя точка получателя

Отдельно от враждебности: цель полёта — `lastKnownLocation`, то есть место выхода игрока, а не
его дом. Если у получателя есть приват, логичнее целиться в него — но это меняет смысл механики
(посылка находит человека или его дом?). Зависит от того, что решено в 3.2.
